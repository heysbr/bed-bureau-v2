import { Button } from "@/components/ui/button";

export default function page() {
  return (
    <div className="flex h-screen justify-center items-center">
      <Button onClick={() => {}}>Click to Throw Error</Button>
    </div>
  );
}
